name: konfigo

services:
    postgres:
        image: postgres:14
        ports:
            - "5432:5432"
        environment:
            POSTGRES_PASSWORD: pwd
            POSTGRES_USER: postgres

    redis:
        image: redis:7.0.6-alpine
        ports:
            - "6379:6379"
